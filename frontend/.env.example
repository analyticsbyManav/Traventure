# Copy this file to .env for local overrides. Never commit .env itself.

# Leave blank for local dev - Vite's proxy handles "/api" -> localhost:5000.
# For production, set this to your deployed backend's URL, including "/api":
#   VITE_API_BASE_URL=https://traventure-backend.onrender.com/api
VITE_API_BASE_URL=
